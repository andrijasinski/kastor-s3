{
  "name": "api",
  "private": true,
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "test": "bun test"
  },
  "dependencies": {
    "hono": "^4",
    "@aws-sdk/client-s3": "^3",
    "@smithy/fetch-http-handler": "^5"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
