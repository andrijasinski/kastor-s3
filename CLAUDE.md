Any endpoint or any business logic unit should have tests written for them in **tests** folder.

---

After any changes to the code, run `bun run feedback`.

---

When working of frontend changes, add screenshot taking to feedback loop. Take a screenshot and make sure that elements are alligned as expected.
